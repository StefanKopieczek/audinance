package com.stefankopieczek.audinance;

/**
 * Class for converting decoded audio from one format to another.
 * Handles resampling, multiplexing and demultiplexing.
 *
 * @author Stefan Kopieczek
 */
public class AudioConverter
{
 	/**
	 * Takes decoded audio in one format and returns it in another,
	 * resampling, multiplexing and demultiplexing as needed.
	 *
	 * @param sourceAudio The original audio data, to be converted.
	 * @param targetFormat The format to convert to.
	 * @return The original audio, converted into the new format.
	 */
	public static DecodedAudio convert(DecodedAudio sourceAudio,
	 						       	   AudioFormat targetFormat)
	{
		return null; //todo
	}
}
